import { SessionData } from "express-session";

export const bla = "";
const session: SessionData = {
  userId: 1,
};

interface User {
  firstName: string;
}

interface User {
  lastName: string;
}

const user: User = {
  firstName: "asdf",
  lastName: "asdf",
};
